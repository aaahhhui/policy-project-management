# Stage 4 企业微信群机器人通知验收记录

日期：2026-08-11（Asia/Shanghai）

分支：`codex/stage4-wecom-notification`

## 验收边界

本记录覆盖六类已批准的通知事件、耐久重试、通知记录、负责人手动重发、密钥零泄漏，以及企业微信内置浏览器中的登录回跳和只读详情体验。测试 Webhook、HTTPS Demo 地址和测试账号只进入本地秘密配置；本文、Git、审计和日志均不记录其字面值。

## 当前证据

| 门禁 | 状态 | 证据 |
| --- | --- | --- |
| 通知记录桌面布局 | PASS（本地 SQLite） | 负责人可筛选、分页、查看安全快照和发送轨迹；终态失败记录显示手动重发。 |
| 通知记录移动布局 | PASS（本地 Chromium 390x844） | 列表、详情快照和发送轨迹可读；手动重发控件隐藏。 |
| 后端完整回归 | PASS | `454 passed, 2 skipped`；包含通知契约、权限、审计、零泄漏和乱序来源任务完成回归。 |
| Ruff / mypy | PASS | `ruff check .` 通过；`mypy app workers` 为 85 个源文件通过。 |
| 前端完整回归与构建 | PASS | 31 个测试文件、141 项测试通过；Vue 类型检查与 Vite 构建退出 0。 |
| 本地服务日志凭据扫描 | PASS | Webhook、Authorization、Cookie 和私钥模式均为 0 匹配。 |
| Docker Compose / MySQL 8.4 | PASS | 独立 `stage4-wecom-verify` 项目中 mysql、api、web、collector、evaluator、notifier、scheduler 均健康；`/api/health` 返回 `status=ok`。 |
| MySQL 迁移往返 | PASS | fresh `0001 -> 0008`，以及 `0008 -> 0007_reconcile_eval_constraint -> 0008` 均退出 0。 |
| MySQL notifier claim / stale claim | PASS | 两个独立 session 对单条待发送记录仅有一个获得 claim；过期 claim 被恢复后，旧 token 无法写入成功。临时记录已按外键顺序清理。 |
| 容器日志凭据扫描 | PASS | Webhook、Authorization、Cookie、私钥和 OpenAI 风格 key 模式均为 0 匹配。 |
| 真实企业微信内置浏览器：通知详情 | PASS | 测试群机器人实际发送成功；手机端打开登录页，以测试负责人账号登录后安全回跳通知详情，页面正常且移动端没有“重新发送”。 |
| 真实企业微信内置浏览器：政策详情 | 未覆盖 | 隔离验收库未采集真实政策；不以通知详情结果替代。 |
| 真实企业微信内置浏览器：项目详情只读 | 未覆盖 | 隔离验收库未创建项目；不以通知详情结果替代。 |

## 代码审查

独立审查覆盖 Stage 4 完整提交范围，发现并修复了“较早创建、较晚完成”的来源采集任务会被错误忽略的问题。修复后的复审没有 Critical 或 Important 问题；不以此替代待执行的 Docker/MySQL 和真实企业微信内置浏览器门禁。

## 真实移动 UAT 清单

1. 用测试 Webhook 触发政策和项目消息，确认群消息仅含受控摘要与详情入口。
2. 在企业微信内置浏览器中以未登录状态打开政策、项目入口，账号密码登录后仅回到允许的相对路径。
3. 在已登录状态重复打开入口，检查政策可读、项目移动端维护控件隐藏，并验证刷新、返回和会话过期后的回跳。
4. 检查地址栏、页面、API 响应、审计行和服务日志，确认没有密钥、Authorization、Cookie 或其他凭据。

## 2026-08-12 真实企业微信验收结果

- 已使用本地秘密配置中的测试群机器人完成实际投递；通知记录状态为发送成功。
- 在企业微信内置浏览器中打开消息后显示登录页；测试负责人完成账号密码登录后安全回跳至通知详情。页面显示正常，移动端未呈现手动重发控件。
- 隔离验收库已初始化公开企业档案（1 条）、经营主体（3 条）和广东省工业和信息化厅来源（1 条），但未将虚构内容写成政策或项目。因此政策详情与项目移动只读场景保留为未覆盖，后续需以真实采集政策和项目消息单独验收。
